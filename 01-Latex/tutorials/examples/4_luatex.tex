To see the law of large numbers in action, let us calculate the average of many random numbers chosen uniformly between $0$ and $1$.
\directlua{
	n = 1000
	sum = 0
	for i=1,n do sum = sum + math.random() end
	tex.print("Average:", sum/n)
}
\vfill \eject \end